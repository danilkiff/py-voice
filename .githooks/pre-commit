#!/usr/bin/env bash
set -euo pipefail

echo "→ ruff fix"
uv run ruff check --fix .
uv run ruff format .

# Re-stage files that ruff may have modified
git diff --name-only | xargs -r git add

echo "→ tests"
uv run pytest --no-header -q
